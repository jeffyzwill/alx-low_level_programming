#include <stdio.h>

/**
 * main - Prints all lower case in reverse
 * Return: 0
 */

int main(void)
{
	int let;

	for (let = 122; (let <= 122) && (let >= 97); let--)
		putchar(let);
	putchar(10);
	return (0);
}
